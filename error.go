package main

import "fmt"

/* import (
	"fmt"
	"io/ioutil"
	"os"
)

func main() {
	files, err := ioutil.ReadDir(".")
	if err != nil {
		fmt.Println(err)
		os.Exit(1)
	}
	for _, value := range files {
		fmt.Println(value.Name())
	}
}
*/

/* //文件写入错误

func proviers(name string) error {
	if f,
} */
